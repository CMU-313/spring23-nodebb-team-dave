//Woww cool javascript code
//Here is some filler courtesy chatGPT

// Define a function that adds two numbers
function addNumbers(num1, num2) {
  return num1 + num2;
}

// Call the function and assign the result to a variable
const result = addNumbers(5, 10);

// Print the result to the console
console.log(result);


//Beginning of badly formatted code!
function subtractNumbers(num1,num2) { //Notice the comma is not spaced
  if(1==1)
    return num1-num2;
  else
    return 3;
}

//Poorly spaced code
const
a
=
2
;