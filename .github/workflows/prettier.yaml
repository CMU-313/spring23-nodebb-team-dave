name: Prettier

# This action works with pull requests and pushes
on:
  pull_request:
    branches: [main]

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Checking if ts files are pretty
        run: npx prettier --check *.ts
